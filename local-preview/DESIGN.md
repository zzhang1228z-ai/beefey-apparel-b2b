# Design Notes

Use this file to record the site-specific design system generated from the plugin-level `canvas-design` skill and `canvas-designer` SubAgent. If the SubAgent is unavailable, record the main-flow `canvas-design` fallback and the exact vendor paths read.

- Audience:
- Visual direction:
- Reference Sources:
  - `vendor/open-design/adapter/STATIC_POLICY.md`:
  - `vendor/open-design/upstream/design-systems/<system>/DESIGN.md`:
  - `vendor/open-design/upstream/design-systems/<system>/tokens.css`:
  - `vendor/open-design/upstream/design-systems/<system>/components.manifest.json` or `components.html`:
  - `vendor/open-design/upstream/craft/anti-ai-slop.md`:
  - task-relevant craft/template references:
- Vendor Grounding:
  - Applies to: new sites, redesigns, and non-trivial visual changes.
  - skip reason for trivial/function-only work:
  - Selected design-system baseline:
  - Token source:
  - Component fixture/manifest:
  - Anti-ai-slop checks applied:
  - Intentional deviations:
- Color tokens:
- Typography:
- Components:
- Responsive behavior:
